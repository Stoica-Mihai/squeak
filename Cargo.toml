[workspace]
resolver = "2"
members = ["crates/squeak-core", "crates/squeak-tui", "crates/squeak-desktop"]
exclude = ["rust-poc"]

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/Stoica-Mihai/squeak"

[workspace.dependencies]
anyhow = "1"
thiserror = "2"
# poll() for timeout reads on hidraw; pure-Rust, keeps the static-binary goal.
rustix = { version = "0.38", features = ["event"] }
# opt-in firmware update check only (network); rustls so the binary stays static.
ureq = { version = "2", features = ["tls", "json"] }
serde_json = "1"
ratatui = "0.29"
squeak-core = { path = "crates/squeak-core" }
