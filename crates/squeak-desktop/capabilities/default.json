{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core APIs (event listen/emit, window) for the squeak window.",
  "windows": ["*"],
  "permissions": ["core:default"]
}
